<UserControl
    x:Class="RadioLib.WaveBandSelectorVIEW"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RadioLib"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ToggleButton Grid.Column="0" Content="LW" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="2" IsChecked="{Binding IsLWOn, Mode=TwoWay}"/>
        <ToggleButton Grid.Column="1" Content="MW" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="2" IsChecked="{Binding IsMWOn, Mode=TwoWay}"/>
        <ToggleButton Grid.Column="2" Content="KW" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="2" IsChecked="{Binding IsKWOn, Mode=TwoWay}"/>
        <ToggleButton Grid.Column="3" Content="UKW" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="2" IsChecked="{Binding IsUKWOn, Mode=TwoWay}"/>
    </Grid>
</UserControl>
